// Este arquivo é a configuração do PostCSS para o projeto.
// Ele define os plugins que serão usados para processar os arquivos CSS, 
// incluindo o Tailwind CSS para gerar as classes utilitárias e o Autoprefixer para garantir compatibilidade entre navegadores.

export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}